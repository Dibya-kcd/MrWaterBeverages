import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mrwater.distributionledger',
  appName: 'MrWater Distribution Ledger',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
